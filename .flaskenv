# Install dotenv via "pip install python-dotenv"; Flask will automatically use this file via command "flask run"
# Only really need to use double quotes for env vars if they contain spaces or special characters, but doesn't hurt to use them
FLASK_APP="app.py"
FLASK_DEBUG="1"
FLASK_RUN_HOST="127.0.0.1"
FLASK_RUN_PORT="5500"
FLASK_ENV="development"